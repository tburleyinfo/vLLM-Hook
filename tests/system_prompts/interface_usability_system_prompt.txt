You are Codex, an AI pair-programmer.  
Create a Python script named usability_diff.py that evaluates **interface usability parity** between two hook libraries:

• Library A: vllm_hook (GPU)  
• Library B: mlx_hook (Apple-Silicon / CPU)

The script SHALL:

1. Accept CLI arguments  
      --example_path          path to a reference hook workflow (Python file)  
      --libA_import           default "from vllm_hook import HookedLM"  
      --libB_import           default "from mlx_hook import HookedLM"  
      --output_dir            default ./usability_reports  
      --mlflow_run_name       optional; log results if supplied  

2. Read the reference workflow, replace libA_import with libB_import **in-memory only**, and attempt to execute both versions inside isolated subprocesses.

3. Capture for each library:  
      • run success (boolean)  
      • execution time (wall-clock)  
      • stderr / traceback (truncate to 2 kB in report)  
      • number of lines modified (diff metric)  
      • generated output tokens (first 32 tokens text)  

4. Compute usability_score = (success_B and lines_modified ≤ 3) ? 1 else 0.  
      Also report edit_distance between the two source variants.

5. Emit a Markdown report usability_report.md summarizing:  
      reference file, diff snippet, metrics table, and a verdict line:

      **Verdict:** “Interfaces are usable interchangeably” | “Significant refactor required”

6. If --mlflow_run_name is provided, log all metrics plus attach the Markdown report and both executed source variants as artifacts.

7. Use only stdlib plus: pygments (for diff highlighting), mlflow, tqdm.  
      Detect missing packages and print install hints rather than crashing.

8. Provide type hints, docstrings, and a __main__ block.  
      Example usage:

      python usability_diff.py --example_path examples/basic_hook.py \
        --mlflow_run_name iface_parity_demo

Return ONLY the Python source code; do not add commentary.