You are Codex, an AI pair-programmer.  

Produce a self-contained Python module named parity_benchmark.py that measures **output consistency** between two language-model endpoints:  

• Endpoint A: GPU-hosted vLLM server  

• Endpoint B: MLX server (CPU or Apple Silicon)  

The script SHALL:

1. Accept CLI arguments  

      --prompt               required user prompt  

      --endpointA            URL or local address for vLLM  

      --endpointB            URL or local address for MLX  

      --max_tokens           default 128  

      --temperature          default 0.0  

      --top_p                default 1.0  

      --seed                 default 42  

      --mlflow_run_name      optional; if supplied, log metrics  

2. For BOTH endpoints  

      • Use identical generation parameters (temperature, top_p, top_k=None, repetition_penalty=1.0, max_tokens).  

      • Disable sampling randomness where possible.  

      • Request raw logits for every timestep and the generated tokens.  

      • Store:  

           prompt_text, input_tokens, output_tokens, logits_per_step.  

3. Compute metrics:  

      exact_text_match              Boolean  

      token_agreement_pct           Percentage of timesteps where argmax token matches  

      avg_logit_cosine              Mean cosine similarity over all timesteps  

      avg_logit_kl                  Mean KL-divergence (softmax) over all timesteps  

      worst_step_cosine             Minimum cosine similarity across timesteps  

4. Print a human-readable summary table and write a JSON report parity_metrics.json.  

5. If --mlflow_run_name is provided, create/run in MLflow and log:  

      prompt_id, parameters, all metrics, and attach the JSON report as an artifact.  

6. Use only standard libraries plus: requests, numpy, scipy, tqdm, mlflow.  

      Guard all imports; exit gracefully with instructions if a package is missing.  

7. Provide a __main__ block so the script can be executed directly.  

      Example usage:  

          python parity_benchmark.py --prompt "Translate 'Hello world' to French" \

          --endpointA http://127.0.0.1:8000 --endpointB http://127.0.0.1:8001 \

          --mlflow_run_name demo_run  

Assume both endpoints expose a JSON API with fields:  

      "tokens": List[int] generated tokens  

      "logits": List[List[float]] logits per timestep (same length as tokens)  

Follow PEP 8. Avoid shortcuts that reduce clarity. Include docstrings and type hints.  

Do not include any private keys or credentials.  

Return ONLY the Python source code; do not wrap it in comments or explanations.